<p align="center">
  <img src="public/flower-randomizer-logo.svg" alt="Flower Randomizer logo" width="108" />
</p>

<h1 align="center">Flower Randomizer</h1>

<p align="center">
  Generative floral artwork with shareable seeds, SVG/PNG export, and dual composition modes.
</p>

<p align="center">
  <a href="#preview">Preview</a> ·
  <a href="#features">Features</a> ·
  <a href="#local-development">Local Development</a> ·
  <a href="#build">Build</a> ·
  <a href="#license">License</a>
</p>

<p align="center">
  中文 / English
</p>

## Overview

`Flower Randomizer` is a small `React` + `Vite` project for generating floral artwork from a seed.
It currently supports both a classic `Bouquet` composition and a more experimental `Abstract` composition.

`Flower Randomizer` 是一个基于 `React` 和 `Vite` 的花艺生成项目。
它支持经典的 `Bouquet` 构图，也支持更自由、更艺术化的 `Abstract` 构图。

## Preview

<p>
  Real outputs generated by the current app.
  当前示例均为项目当前版本的真实生成结果。
</p>

<p align="center">
  <img src="docs/preview-1.svg" alt="Bouquet preview 1" width="48%" />
  <img src="docs/preview-2.svg" alt="Bouquet preview 2" width="48%" />
</p>

<p align="center">
  <img src="docs/preview-3.svg" alt="Abstract preview 1" width="48%" />
  <img src="docs/preview-4.svg" alt="Abstract preview 2" width="48%" />
</p>

<p align="center">
  <sub>Repository social preview asset: <a href="docs/social-preview.png">docs/social-preview.png</a></sub>
</p>

## Features

- Generate floral artwork from deterministic seeds
- Switch between `Bouquet` and `Abstract` composition modes
- Restore and share the current artwork through the URL
- Export artwork as `SVG` or `PNG`
- Lightweight frontend-only project with no backend dependency

## Local Development

### Requirements

- `Node.js`
- `npm`

### Setup

```bash
npm install
npm run dev
```

On Windows, you can also run:

```bat
start.bat
```

## Build

```bash
npm run build
```

The production build is written to `dist/`.

## Regenerating Preview Images

If you change the generator and want to refresh the README showcase images, run:

```bash
node scripts/generate-previews.mjs
```

The preview images are written to `docs/`.

## Project Structure

```text
src/
  App.jsx                Main app shell and interactions
  ArtworkCard.jsx        Artwork rendering
  flowerGenerator.js     Composition and floral generation logic
  exportArtwork.js       SVG/PNG export helpers
  shareState.js          Share-link serialization and parsing
scripts/
  generate-previews.mjs  Generates README preview images
docs/
  preview-*.svg          Generated showcase images
  social-preview.png     Repository social preview image
public/
  flower-randomizer-logo.svg  Brand logo and favicon source
```

## Deployment

This is a static frontend app and can be deployed on any static hosting platform such as `Vercel`, `Cloudflare Pages`, or `GitHub Pages`.

## License

This project is licensed under the `MIT` License.
